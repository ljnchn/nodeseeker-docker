{
  "name": "nodeseeker",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/server.ts",
    "build": "bun build src/server.ts --outdir dist --target bun",
    "start": "bun run dist/server.js",
    "db:migrate": "bun run src/database/run-migrations.ts",
    "db:reset": "bun run src/database/reset-database.ts",
    "test": "bun test"
  },
  "dependencies": {
    "hono": "^4.9.2",
    "node-cron": "^4.2.1",
    "grammy": "^1.30.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/bcryptjs": "^2.4.6",
    "@types/jsonwebtoken": "^9.0.5",
    "@types/node": "^20.10.5"
  }
}
