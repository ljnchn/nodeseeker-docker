# NodeSeeker Docker 环境变量配置模板
# 复制此文件为 .env 并修改相应的值

# ===========================================
# 服务器配置
# ===========================================
PORT=3010
HOST=0.0.0.0
NODE_ENV=production
# 允许的跨域源，多个用逗号分隔
CORS_ORIGINS=http://localhost:3010,https://your-domain.com

# ===========================================
# 数据库配置
# ===========================================
DATABASE_PATH=./data/nodeseeker.db

# ===========================================
# RSS 配置
# ===========================================
RSS_URL=https://rss.nodeseek.com/
RSS_TIMEOUT=10000
RSS_USER_AGENT=NodeSeeker-Bot/1.0
RSS_CHECK_ENABLED=true
# RSS 检查频率 (cron 表达式): 每分钟检查一次
RSS_CRON_EXPRESSION=*/1 * * * *

# ===========================================
# 数据清理配置
# ===========================================
DATA_CLEANUP_ENABLED=true
# 数据清理频率 (cron 表达式): 每天凌晨2点执行
CLEANUP_CRON_EXPRESSION=0 2 * * *
# 数据保留天数
DATA_RETENTION_DAYS=30

# ===========================================
# Telegram Bot 配置 (可选)
# ===========================================
# 从 @BotFather 获取的 Bot Token
# TELEGRAM_BOT_TOKEN=your-telegram-bot-token

# Webhook URL (生产环境使用，开发环境可留空)
# 格式: https://your-domain.com/telegram/webhook
# TELEGRAM_WEBHOOK_URL=https://your-domain.com/telegram/webhook

# ===========================================
# 日志配置
# ===========================================
LOG_LEVEL=info

# ===========================================
# Docker 部署注意事项
# ===========================================
# 1. 使用固定 Token 认证，无需额外配置
# 2. 生产环境建议修改默认端口
# 3. CORS_ORIGINS 需要设置为实际域名
# 4. Telegram 配置可以通过 Web 界面设置